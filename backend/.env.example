## SmartPresence Backend (.env)
## Copy as backend/.env for local dev, or set these variables in Render.

# Server
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1

# Database (PostgreSQL)
# Example: Supabase/Render Postgres connection string
DATABASE_URL=postgresql://postgres:password@db.host:5432/postgres

# JWT
JWT_SECRET=change-me-please-min-32-chars
JWT_EXPIRES_IN=7d

# CORS (optional; only needed for a web panel)
# Comma-separated origins. Example:
# CORS_ORIGINS=http://localhost:5173,https://panel.tudominio.com
CORS_ORIGINS=
CORS_CREDENTIALS=false

# Rate limiting (optional)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=300

# TypeORM tuning (optional)
# In production you should keep TYPEORM_SYNCHRONIZE=false.
TYPEORM_SYNCHRONIZE=false
TYPEORM_LOGGING=false
DB_SSL=true
DB_CONNECT_TIMEOUT_MS=5000
